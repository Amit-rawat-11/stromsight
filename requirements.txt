# StormSight — Python Dependencies
# Install: pip install -r requirements.txt

# Core ML/DL
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# Image Processing (for real satellite image loading)
pillow>=10.0.0

# HTTP Requests (for Open-Meteo live API)
requests>=2.31.0
